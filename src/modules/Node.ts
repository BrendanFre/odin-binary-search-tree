export default function Node(defineData: any) {
  const data = defineData;
  const leftChild: number | null = null;
  const rightChild: number | null = null;

  return { data, leftChild, rightChild };
}
